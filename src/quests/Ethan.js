// write a function that takes a promise, and returns another promise that will throw after 5 seconds...

const expiringPromise = (promise) => {
  // return a new promise that will throw with like... "Expired" after 5 seconds
}
